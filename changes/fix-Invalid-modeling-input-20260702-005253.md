Invalid modeling input now errors instead of silently doing nothing (#104, #109, #110, #112): scripted zero-size rect/circle/line and zero-distance extrude are rejected; fillet_vertex/chamfer_vertex on a vertex that doesn't join exactly two lines, or on a corner within ~1° of straight, errors; extrude rejects faces with missing/non-loop line indices instead of creating a dead extrusion. Failed declarative Lua calls now raise (pcall-able) script errors; the GUI keeps reporting the same messages in the status bar.
