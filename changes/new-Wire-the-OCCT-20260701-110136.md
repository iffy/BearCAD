Wire the OCCT kernel into CI and releases (#89): a cached `occt` CI job builds OCCT once and runs the --features occt test suite (the default no-kernel build is still tested too); macOS and Linux release binaries now ship with the kernel linked. Windows ships the hand-rolled fallback build (no static OCCT/MSVC build yet). The occt feature stays off in Cargo's default set so the common dev/CI build needs no C++ toolchain.
