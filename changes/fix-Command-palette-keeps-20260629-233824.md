Command palette keeps keyboard focus on the input when the query matches nothing; previously the input silently lost focus (requiring a click) when the results area swapped to 'No matching commands'.
