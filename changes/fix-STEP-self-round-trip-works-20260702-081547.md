STEP self-round-trip works end to end (#106): the faceted writer now emits a conformant complex context entity and full AP203 product scaffolding (OCCT and third-party readers can parse and transfer BearCAD's own files); a single-body whole-document export writes real BREP so curved fillets survive the round-trip exactly; and open/save/import/export failures now raise catchable Lua errors instead of silently succeeding with an empty document.
