The OCCT kernel is now ON by default, so the normal `cargo build`/`cargo run` ships the real kernel — fixes extrude Cut (and true fillets, curved STEP) being invisible in the default build (#95), and completes the default-flip of #89. The lean no-kernel fallback builds with `--no-default-features` (used by the Windows release + the fast CI check); the default build now requires a built OCCT (scripts/build-occt.sh).
