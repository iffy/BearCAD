`name` on a solid verb now names the body the call hands back as well as the operation, so the returned handle carries the name and `find` gives that handle back; `shape_name` and `body_name` name the two apart.
