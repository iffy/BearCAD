Viewport rendering fixes: #101 wireframe no longer draws every facet seam on tessellated-smooth surfaces (crease threshold loosened from ~1.1 deg to 15 deg, shared with body-edge picking so pickable edges match drawn edges); #102 realistic shading adds a camera headlight diffuse term so camera-facing surfaces are always clearly lit (about as bright as Solid mode) instead of dropping to the ambient floor.
