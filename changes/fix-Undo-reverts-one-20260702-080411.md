Undo reverts one whole user action (#105): the creation-order list now records how many entries each action created (undo groups, persisted in the file), so undoing a rectangle removes its 4 lines and all constraints in a single step instead of ~14 presses that first silently ate its dimensions. Group accounting self-reconciles for legacy files and net-zero mutations (e.g. chamfer replacing a constraint entry with a bridge line).
