2D extrude-face boolean regions (#16/#62) are computed by the OCCT kernel in kernel builds (#88): a planar-face Cut/Common with a strict one-hole-free-face contract, behind the single `face_boolean` seam; the hand-rolled Weiler-Atherton clipper remains only as the --no-default-features fallback (deletable with #96) and an occt-gated parity test matrix keeps the two paths agreeing.
