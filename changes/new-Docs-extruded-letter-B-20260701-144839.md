Docs: extruded letter-B Line-tool example (docs-site/screenshots/letter-b.lua) — its outer silhouette is extruded to a solid and its two counters punched as real OCCT boolean cuts (body = "cut") so it unmistakably reads as a capital B. Kernel-agnostic regression test (volume/bbox/triangle-count) plus an occt-only test asserting the cut volume; the docs screenshots CI job now uses the default kernel build.
